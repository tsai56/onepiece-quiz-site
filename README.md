# 樂高® 航海王人格測驗 prototype

此版本更新：
- 背景左上角加入 LEGO® x ONE PIECE 活動 logo
- 網站背景改為使用指定的航海王主視覺圖，滿版呈現
- 保留 LEGO® 正式 logo
- 首頁、規則頁、題目頁、結果頁統一套用滿版背景
- 題目選項隨機排序
- 結果頁直接顯示推薦盒組
- 下載結果圖也會帶入同一張背景圖

## 測試方式
直接以瀏覽器開啟 `index.html`。

## 正式上線前可修改
- `script.js` 第一行 `FACEBOOK_POST_URL`
- `styles.css` 中的背景與色彩設定

- 內文左上 logo 改為 6.png，並同步套用在下載結果圖

- 角色圖大小已依 ref 1 縮小，避免搶走主標視覺。

- 依照最新 ref 放大首頁上方角色圖，提升存在感。

- 左上角色圖依最新 ref 再放大，提升主視覺存在感。

- 依 ref 01 的畫面比例，首頁左上角色圖桌機寬度調整為 480px，手機版 240px。

## 檔案結構更新

所有圖檔已整理至 `img/` 資料夾，HTML、CSS、JavaScript 內的圖檔路徑已同步調整。

- 角色圖 6.png 已將上下留白各縮小約 1/2。

- 首頁 eyebrow 改為「樂高®航海王之偉大航道開測！」並加大字級。
- 首頁主標「你是哪一位草帽船員？」行距加大。

- 「偉大航道開測！」已改為「樂高®航海王之偉大航道開測！」，並加大字級 2 級。

- 首頁主標「你是哪一位草帽船員？」行距加大 2 級。

- 已移除首頁右上角「航海王人格測驗」標籤。

- 規則頁 1–5 文字已改為指定 4 點，其餘內容不調整。

- 規則第 4 點已依指定位置斷行。

- 左上角 campaign logo 已替換為使用者提供的新圖，尺寸設定不變。

- 第二題答案 B 已改為「嘴上說完蛋了，手已經開始出招」。
- 左上角 logo 已設定為可點擊回首頁。

- 已修正 copyright：改為放入每個頁面區塊的正常排版流，固定在內容框下方 30px，不覆蓋按鈕，也不影響開始測驗按鈕。

- Copyright 與黃色內容框間距已由 30px 改為 20px，其他內容不變。

- 結果頁調整：左上紅字改為置中並放大 1/2、人格關鍵字前新增紅框標題、移除「小樂推薦你的冒險盒組」內容框。

- 修正結果頁「下載結果圖」：改用 async 圖片載入與 canvas.toBlob 下載，避免按鈕失效。

- 再次修正結果頁「下載結果圖」：改為明確綁定 click 事件，並提供 toBlob / toDataURL 兩種下載路徑。

- 下載結果圖改為同步產生，不再等待圖片載入，避免 iOS/Safari/Chrome 因失去使用者點擊手勢而阻擋下載。

- 結果頁下載圖版型調整：人格關鍵字改為與結果頁一致的標籤 + 關鍵字膠囊樣式，並縮小各內容區塊上下間距。

- 修正下載圖中 emoji 於 canvas 被瀏覽器放大錯誤渲染的問題：下載圖改用文字標記，不再繪製 emoji。

- 下載圖重新撰寫：完全移除 emoji、外部圖片與裝飾圖形，只用文字、色塊與框線繪製，避免再次出現巨大白色圖形。

- 依照「樂高航海王人格測驗_娜美型.png」重製下載結果圖版型：沿用原本文案，只同步字級、顏色、間距與尺寸風格。
- 新增 6 張對應角色結果圖：魯夫、騙人布、娜美、喬巴、香吉士、索隆，下載時依結果自動套用。

- 修正下載結果圖無反應：改用 document click delegation 綁定下載鈕，並將下載流程改為同步，不再 await 圖片載入。

- 修正首頁「開始測驗」無反應：移除破損按鈕綁定片段，改用 document click delegation 處理所有 data-go 導頁按鈕，並通過 JS syntax check。

- 已改為使用使用者提供的 6 張最終 JPG 結果頁，下載結果圖時不再經過 canvas 重新產圖。
- 下載按鈕會依測驗結果直接下載對應 JPG：魯夫 / 索隆 / 娜美 / 香吉士 / 騙人布 / 喬巴。
- 下載完成後會跳出提示訊息：「結果圖已下載，記得回到臉書活動貼文留言上傳！」。

- 下載結果圖改為另開新分頁顯示對應 JPG，原活動網站分頁保留；開啟後顯示提醒訊息。

- 修正下載結果圖按鈕樣式：維持原本紅色按鈕設計，文字置中並移除連結底線。

- 移除下載結果圖的 JS 攔截與「瀏覽器阻擋新分頁」alert，改為原生 <a target="_blank"> 直接另開結果圖分頁。

- 結果頁上方角色圖已改為依照各結果對應顯示使用者提供的 6 張角色圖（魯夫 / 索隆 / 娜美 / 香吉士 / 騙人布 / 喬巴）。

- 結果頁角色圖已改為置中。
- 6 張上傳角色圖已對應各結果頁。
- 上傳角色圖顯示尺寸調整為原圖約 1/2。

- 修正結果頁角色圖未置中、未縮小的問題（CSS 已重寫）。

- 6 張結果頁角色圖改為視覺高度完全一致（固定高度置中顯示）。
- 角色圖整體再放大約 30%。

- 角色圖與上下內容的垂直間距縮小 50%。

- 角色圖上下間距再縮小，調整為約原始間距的 30%（約縮 70%）。

- 結果頁最下方「樂高® 航海王人格測驗」已移除，其餘內容維持不變。


本版調整：
- 僅修改桌機版結果頁角色圖上下間距，縮小 50%。
- 手機版維持不變。
- 其他內容不變。


本次追加調整：
- 喬巴型結果頁角色圖已更換為最新提供圖片。
- 其他內容維持不變。


本次追加調整：
- 首頁所有資訊改為置中。
- 首頁最上方角色圖放大（約 200%）。

- 首頁最上方角色圖由前一版縮小 20%，其他內容維持不變。

- 僅修改手機版首頁：最上方角色圖放大約 30%。桌機版與其他頁面不變。

- 首頁副標移除「航線」後方逗點。
- 首頁說明文字依指定位置加入斷行，未調整字體顏色與大小。

- 活動說明頁：僅修改文案，格式/版型維持舊版（如參考圖）。

- 活動說明頁：標題改為「完成測驗（斷行）找出你的草帽船員人格」，行距比照首頁文案；其餘不變。

- 題目頁文案更新：Q1、Q2、Q4、Q8 依需求校正；其中 Q2-E 改為「嘴上說完蛋了，但手已經開始想招」，Q4 改為「如果你是草帽海賊團成員，最想擔任什麼職務？」。
- 題目頁標題行距改為與首頁一致。

- 首頁說明文字依指定斷行更新。
- 遊戲說明頁規則文案依指定內容更新。
- 其他內容維持上一版不變。

- 首頁角色圖與內容框上方間距縮小 50%。
- 其他內容維持上一版不變。

- 題目 Q8 文案更新為：抽到樂高®喬巴帽周邊商品時，你會？
- 其他內容不變。

- 6 個結果頁底部分享文案改為依照結果名稱動態顯示：
  「截圖你的_____型結果，回到粉絲專頁貼文留言上傳，就有機會獲得樂高®喬巴帽周邊商品」
- 其餘內容維持不變。

- 香吉士型副標「帥氣不是人設，是基本禮貌。」移除句號，其他內容不變。

- 其餘五個結果頁副標句尾句號已移除（魯夫、索隆、娜美、騙人布、喬巴），其他內容不變。

- 已更新 6 張「下載結果頁」JPG，改為使用最新提供的結果頁版本（騙人布 / 娜美 / 魯夫 / 喬巴 / 香吉士 / 索隆）。
